{
  "name": "voicerails-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "voicerails": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint configured for voicerails-cli\"",
    "test": "echo \"No tests yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@voicerails/sdk": "workspace:*"
  }
}
